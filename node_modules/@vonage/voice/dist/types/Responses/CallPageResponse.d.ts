import { APILinks } from '@vonage/server-client';
import { CallDetailResponse } from '@vonage/voice/dist/types';
export type CallPageResponse = {
    count: number;
    page_size: number;
    record_index: number;
    _embedded: {
        calls: Array<CallDetailResponse>;
    };
} & APILinks;
