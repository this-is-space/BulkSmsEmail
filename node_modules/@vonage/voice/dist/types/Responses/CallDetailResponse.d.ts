import { APILinks } from '@vonage/server-client';
import { CallDetail } from '@vonage/voice/dist/types/CallDetail';
import { CallEndpointResponse } from '@vonage/voice/dist/types/Responses/CallEndpointResponse';
export type CallDetailResponse = {
    conversation_uuid: string;
    start_time: string;
    end_time: string;
    to: CallEndpointResponse;
} & Omit<CallDetail, 'conversationUUID' | 'startTime' | 'endTime' | 'to' | 'from'> & APILinks;
