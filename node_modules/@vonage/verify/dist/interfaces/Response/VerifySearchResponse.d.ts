import { SearchCheckInformationResponse } from '@vonage/verify/dist/interfaces/Response/SearchCheckInformationResponse';
import { SearchEventInformationResponse } from '@vonage/verify/dist/interfaces/Response/SearchEventInformationResponse';
import { SearchStatus } from '@vonage/verify/dist/enums';
export interface VerifySearchResponse {
    request_id: string;
    account_id: string;
    status: SearchStatus;
    number: string;
    price: string;
    currency: string;
    sender_id: string;
    date_submitted: string;
    date_finalized: string;
    first_event_date: string;
    last_event_date: string;
    checks: SearchCheckInformationResponse[];
    events: SearchEventInformationResponse[];
    estimated_price_messages_sent: string;
}
