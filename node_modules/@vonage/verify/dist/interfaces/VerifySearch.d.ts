import { VerifySearchResponse } from '@vonage/verify/dist/interfaces/Response';
import { VerifySearchCheck } from '@vonage/verify/dist/interfaces/VerifySearchCheck';
export interface VerifySearch extends Omit<VerifySearchResponse, 'checks'> {
    requestId: string;
    accountId: string;
    senderId: string;
    dateSubmitted: string;
    dateFinalized: string;
    firstEventDate?: string;
    lastEventDate?: string;
    checks: VerifySearchCheck[];
    estimatedPriceMessagesSent: string;
}
