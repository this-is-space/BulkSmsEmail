import { VerificationParameters } from '@vonage/verify/dist/types/VerificationParams';
export type PSD2Parameters = Omit<VerificationParameters, 'brand' | 'senderId' | 'pinCode'> & {
    payee: string;
    amount: number;
};
