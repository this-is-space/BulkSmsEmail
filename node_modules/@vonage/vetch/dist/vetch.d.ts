import { VetchResponse } from '@vonage/vetch/dist/interfaces/vetchResponse';
import { VetchOptions } from '@vonage/vetch/dist/interfaces/vetchOptions';
export declare class Vetch {
    defaults: VetchOptions;
    constructor(defaults?: VetchOptions);
    private _defaultAdapter;
    request<T>(opts?: VetchOptions): Promise<VetchResponse<T>>;
    private getResponseData;
    private validateOpts;
    private checkStatus;
    private createResponse;
}
