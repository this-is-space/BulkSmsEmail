/// <reference types="node" />
/// <reference types="node" />
/// <reference types="node" />
import { HTTPMethods } from '@vonage/vetch/dist/enums/HTTPMethods';
import { Headers } from '@vonage/vetch/dist/interfaces/headers';
import { ResponseTypes } from '@vonage/vetch/dist/enums/responseTypes';
import http from 'http';
import https from 'https';
import { VetchPromise } from '@vonage/vetch/dist/types/vetchPromise';
export interface VetchOptions {
    adapter?: <T = any>(options: VetchOptions, defaultAdapter: (options: VetchOptions) => VetchPromise<T>) => VetchPromise<T>;
    url?: string;
    baseUrl?: string;
    baseURL?: string;
    method?: HTTPMethods;
    headers?: Headers;
    data?: any;
    body?: any;
    params?: any;
    responseType?: ResponseTypes;
    checkStatus?: (status: number) => boolean;
    size?: number;
    timeout?: number;
    appendUserAgent?: string;
    agent?: boolean | http.Agent | https.Agent | ((parsedUrl: URL) => boolean | https.Agent | http.Agent);
}
