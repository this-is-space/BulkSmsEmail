import { Headers } from '@vonage/vetch/dist/interfaces/headers';
import { VetchHttpRequest } from '@vonage/vetch/dist/interfaces/vetchHttpRequest';
import { VetchOptions } from '@vonage/vetch/dist/interfaces/vetchOptions';
export interface VetchResponse<T> {
    config: VetchOptions;
    data: T;
    error?: true;
    status: number;
    statusText: string;
    headers: Headers;
    request: VetchHttpRequest;
}
