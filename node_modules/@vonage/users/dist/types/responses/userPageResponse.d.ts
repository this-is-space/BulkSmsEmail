import { APILinks } from '@vonage/server-client';
import { UserResponse } from '@vonage/users/dist/types/responses/userResponse';
export type UserPageResponse = {
    page_size: number;
    _embedded: {
        users: Array<UserResponse>;
    };
} & APILinks;
