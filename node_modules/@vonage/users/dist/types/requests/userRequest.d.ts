import { UserType } from '@vonage/users/dist/types';
import { WebSocketChannelRequest } from '@vonage/users/dist/types/requests/websocketChannelRequest';
export type UserRequest = {
    image_url: string;
    properties: {
        custom_data: Record<string, string>;
    };
    channels: {
        websocket: Array<WebSocketChannelRequest>;
    } & Omit<Pick<UserType, 'channels'>, 'websocket'>;
} & Omit<UserType, 'imageUrl' | 'properties'>;
