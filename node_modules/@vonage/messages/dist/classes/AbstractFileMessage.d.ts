import { AbstractMessage } from '@vonage/messages/dist/classes/AbstractMessage';
import { MessageFileInterface } from '@vonage/messages/dist/interfaces';
import { MessageFileType, MessageParamsFile } from '@vonage/messages/dist/types';
export declare abstract class AbstractFileMessage extends AbstractMessage implements MessageFileInterface {
    messageType: 'file';
    file: MessageFileType;
    protected constructor(params: MessageParamsFile);
}
