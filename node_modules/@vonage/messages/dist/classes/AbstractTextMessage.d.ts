import { AbstractMessage } from '@vonage/messages/dist/classes/AbstractMessage';
import { MessageTextInterface } from '@vonage/messages/dist/interfaces';
import { MessageParamsText } from '@vonage/messages/dist/types';
export declare abstract class AbstractTextMessage extends AbstractMessage implements MessageTextInterface {
    messageType: 'text';
    text: string;
    constructor(params: MessageParamsText);
}
