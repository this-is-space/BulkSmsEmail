import { AbstractImageMessage } from '@vonage/messages/dist/classes/AbstractImageMessage';
import { WhatsAppImageInterface } from '@vonage/messages/dist/interfaces';
import { WhatsAppImageParams } from '@vonage/messages/dist/types';
export declare class WhatsAppImage extends AbstractImageMessage implements WhatsAppImageInterface {
    channel: 'whatsapp';
    constructor(params: WhatsAppImageParams);
}
