import { AbstractMessage } from '@vonage/messages/dist/classes/AbstractMessage';
import { WhatsAppTemplateInterface } from '@vonage/messages/dist/interfaces';
import { WhatsAppTemplateParams } from '@vonage/messages/dist/types';
import { WhatsAppPolicyType } from '@vonage/messages/dist/types';
import { WhatsAppTemplateType } from '@vonage/messages/dist/types';
export declare class WhatsAppTemplate extends AbstractMessage implements WhatsAppTemplateInterface {
    channel: 'whatsapp';
    messageType: 'template';
    whatsapp: WhatsAppPolicyType;
    template: WhatsAppTemplateType;
    constructor(params: WhatsAppTemplateParams);
}
