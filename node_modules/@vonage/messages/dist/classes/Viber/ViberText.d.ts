import { ViberTextInterface } from '@vonage/messages/dist/interfaces';
import { AbstractTextMessage } from '@vonage/messages/dist/classes/AbstractTextMessage';
import { ViberActionParams } from '@vonage/messages/dist/types';
import { ViberTextParams } from '@vonage/messages/dist/types';
export declare class ViberText extends AbstractTextMessage implements ViberTextInterface {
    channel: 'viber_service';
    viberService: ViberActionParams;
    constructor(params: ViberTextParams);
}
